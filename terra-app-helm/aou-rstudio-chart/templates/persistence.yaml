{{- if .Values.persistence.enabled -}}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "aou-rstudio-app.fullname" . }}-pv
spec:
  storageClassName: "{{ .Values.persistence.storageClass }}"
  capacity:
    storage: {{ .Values.persistence.size }}
  accessModes:
    - {{ .Values.persistence.accessMode }}
  {{- if .Values.persistence.hostPath }}
  hostPath:
    path: {{ .Values.persistence.hostPath }}
  {{- end }}
  claimRef:
    namespace: {{ $.Release.Namespace }}
    name: {{ include "aou-rstudio-app.fullname" . }}-pvc
  {{- if .Values.persistence.gcePersistentDisk }}
  gcePersistentDisk:
    pdName: {{ .Values.persistence.gcePersistentDisk }}
    fsType: ext4
  {{- end }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "aou-rstudio-app.fullname" . }}-pvc
spec:
  storageClassName: "{{ .Values.persistence.storageClass }}"
  volumeName: {{ include "aou-rstudio-app.fullname" . }}-pv
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- end }}
