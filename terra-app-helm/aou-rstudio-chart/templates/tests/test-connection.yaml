apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aou-rstudio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "aou-rstudio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aou-rstudio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
