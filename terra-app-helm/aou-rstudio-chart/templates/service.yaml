apiVersion: v1
kind: Service
metadata:
  name: {{ include "aou-rstudio-chart.fullname" . }}
  labels:
    {{- include "aou-rstudio-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aou-rstudio-chart.selectorLabels" . | nindent 4 }}
